{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker:run": "docker run -d -p 8082:3000 page-home:1.0",
    "docker:build": "docker build -t page-home:1.0 . && npm run docker:run"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@types/node": "^18.16.19",
    "nuxt": "^3.6.5"
  },
  "dependencies": {
    "http-proxy-middleware": "^2.0.6",
    "nuxt-swiper": "^1.2.0",
    "sass": "^1.64.1",
    "wow.js": "^1.2.2"
  }
}
